namespace HorusUITest.Enums
{
    public enum IndexType
    {
        /// <summary>
        /// Absolute index, disregarding the current context.
        /// </summary>
        Absolute,
        /// <summary>
        /// Index relative to the current context, e.g. currently visible items on the screen.
        /// </summary>
        Relative
    }
}
